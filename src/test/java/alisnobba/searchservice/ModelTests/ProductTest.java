package alisnobba.searchservice.ModelTests;

public class ProductTest {
   /* @Test
    public void getPrettyDurationTest() {
        ProductForOrder myProduct = new ProductForOrder("blue men cotton shirt","Men", "Shirts", 10.00, "M", 10);
        String categoryResult = myProduct.getCategory();
        String expectedResult="Men";
        junit.framework.Assert.assertEquals("getDurationPretty should convert a song's duration in seconds into the format mm:ss",categoryResult,expectedResult);
    }*/
}
